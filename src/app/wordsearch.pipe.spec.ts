import { WordsearchPipe } from './wordsearch.pipe';

describe('WordsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new WordsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
